// Prisma client placeholder - connects to PostgreSQL when configured
// For demo, the app uses mock data from src/lib/mock-data.ts
// To enable database: configure DATABASE_URL and run prisma migrate dev

export const prisma = null as unknown as Record<string, unknown>;
export default prisma;
